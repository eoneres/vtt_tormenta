---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "compendium-service.fullname" . }}
  labels:
    {{- include "compendium-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compendium-service.selectorLabels" . | nindent 4 }}
