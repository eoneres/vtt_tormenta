# API Gateway URL (public — exposed to browser)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Colyseus WebSocket URL (public — exposed to browser)
NEXT_PUBLIC_REALTIME_URL=ws://localhost:2567

# Node environment
NODE_ENV=development
